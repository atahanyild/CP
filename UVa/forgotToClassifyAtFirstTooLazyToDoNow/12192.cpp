#include <stdio.h>
#include <algorithm>

using namespace std;

int main()
{
  int r, c;
  scanf("%d %d", &r, &c);
  while (r || c)
  {
    int ar[r][c];
    for (int i = 0; i < r; i++)
      for (int j = 0; j < c; j++)
      {
        scanf("%d", &ar[i][j]);
      }

    int lp = 0, rp = c, mp = c / 2, res;
    while (lp <= rp)
    {
        }
  }
}